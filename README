這是一支在Android上運行的小程式
會透過網路連線連到我自己寫的一個網頁
http://www.mgt.ncu.edu.tw/~jerry54010/get_data.php

透過該網頁去對中央大學即時氣象的網頁做分析
並抓取資料下來，顯示在手機端上

分析資料的方法只要該網頁無法讀取即會失敗 
(例外處理將會寫在網頁端，手機端只會接收到error)

另外有Widget可以使用

點擊Widget左方為進入主程式
點擊Widget右方為更新資料
當手機解鎖時則會自動更新資料一次